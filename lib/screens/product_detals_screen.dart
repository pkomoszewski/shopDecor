import 'package:flutter/material.dart';
class ProductDetalsScreen extends StatelessWidget {
  const ProductDetalsScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("cos")),
      body: Column(),
    );
  }
}